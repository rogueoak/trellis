# Thin wrapper: the command body is the shared, tool-neutral skill, injected at runtime so there
# is exactly one source of truth (the same SKILL.md Claude / Codex / Cursor run).
description = "Update an installed repo's Trellis rules to the plugin version"
prompt = """
@{skills/trellis-update/SKILL.md}
"""
